Flask>=3.0.0
flask-smorest>=0.44.0
marshmallow>=4.0.0

# Auth
# Authlib's Flask integration is built on requests_client, but Authlib itself
# doesn't declare requests as a hard dependency — pin it explicitly.
Authlib>=1.3.0
requests>=2.31.0

# Persistence
SQLAlchemy>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.2.0

# Observability
# HTTP/protobuf exporters, not gRPC: Grafana Cloud's OTLP gateway is an
# HTTPS endpoint, and gRPC needs network setup (HTTP/2, long-lived
# connections) that doesn't fit Render's free-tier networking as cleanly.
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
opentelemetry-instrumentation-flask>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
